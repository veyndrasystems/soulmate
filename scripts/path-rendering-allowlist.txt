# Absent --root defaults are CLI option defaults, not path conversion.
src/cli.rs:a.options.get("root").map(String::as_str).unwrap_or("."),
src/project_commands.rs:.unwrap_or(".");
# Human display fallback only; the separate command conversion below stays strict.
src/project_commands.rs:let value = path.to_string_lossy();
